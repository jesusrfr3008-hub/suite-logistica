Flask==3.0.3
Flask-SQLAlchemy==3.1.1
# Ronda AV (2026-09-25, corrección, a pedido del usuario): antes no se
# fijaba la version de SQLAlchemy (solo la de Flask-SQLAlchemy) -- un
# despliegue nuevo en Railway instalo sola la SQLAlchemy mas nueva
# (2.1.0), que cambio el driver Postgres POR DEFECTO de psycopg2 a
# psycopg v3 para el esquema "postgresql://" a secas, y la app crasheo
# ("ModuleNotFoundError: No module named 'psycopg'") sin que nadie hubiera
# tocado el codigo. Se fija 2.0.54 (la misma con la que esta app se probo
# de punta a punta) para que esto no vuelva a cambiar solo en un
# despliegue futuro -- ademas, app.py ahora fuerza el driver "+psycopg2"
# explicitamente en el esquema de conexion, como segunda capa de
# seguridad independiente de esta version.
SQLAlchemy==2.0.54
Flask-Login==0.6.3
openpyxl==3.1.5
xhtml2pdf==0.2.16
pywin32>=306; sys_platform == "win32"
# Ronda M (2026-09-10): dependencias para el despliegue en Railway --
# psycopg2-binary conecta con Postgres (DATABASE_URL, ver app.py) y gunicorn
# es el servidor de produccion que arranca la app ahi (ver Procfile). Ninguna
# de las dos afecta el uso local en Windows (sys_platform sigue igual).
# 2.9.9 no trae wheel precompilado para la version de Python que usa el
# builder de Railway y el build fallaba tratando de compilarlo desde
# codigo fuente (faltan headers de gcc/postgres en esa imagen) -- 2.9.10
# si trae wheel para esa version, se sube sin compilar nada.
psycopg2-binary==2.9.10
gunicorn==23.0.0
# Ronda AS (2026-09-24): "hora de Chile" en las pantallas de "última
# actualización" (ver filtro hora_cl / zoneinfo en app.py) -- zoneinfo ya es
# parte de Python 3.9+, pero busca la base de datos de husos horarios del
# sistema operativo, que puede faltar en la imagen mínima de Railway. tzdata
# la trae como paquete Python, sin depender de lo que tenga instalado el
# contenedor.
tzdata==2024.2
